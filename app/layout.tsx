import type { Metadata } from 'next';
import './globals.css';
import { ThemeProvider } from '@/components/ThemeProvider';

export const metadata: Metadata = {
  title: 'Lakhaan - Free GST Invoice Generator',
  description: 'Create GST invoices for free in seconds. Simple, fast, and reliable billing software for small businesses.',
  keywords: 'GST Invoice, Free Invoice Generator, Billing Software, Small Business',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="antialiased">
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
